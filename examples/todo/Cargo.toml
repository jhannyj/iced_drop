[package]
name = "todo"
version = "0.1.1"
edition = "2024"

[dependencies]
iced_drop = { path = "../.." , features = ["helpers"]}
iced = { git = "https://github.com/iced-rs/iced.git", features = ["advanced"] }
